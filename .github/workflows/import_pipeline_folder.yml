name: Deploy Fabric Pipeline

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy-pipeline:
    runs-on: ubuntu-latest
    env:
      CLIENT_ID: ${{ secrets.CLIENT_ID }}
      CLIENT_SECRET: ${{ secrets.CLIENT_SECRET }}
      TENANT_ID: ${{ secrets.TENANT_ID }}
      FABRIC_WORKSPACE_ID: ${{ secrets.FABRIC_WORKSPACE_ID }}
      FABRIC_WORKSPACE_NAME: Prod-Fabric-workspace

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Set up Python 3.11
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install ms-fabric-cli
        run: |
          pip install --upgrade pip
          pip install ms-fabric-cli

      - name: Login to Fabric CLI
        run: |
          fab config set encryption_fallback_enabled true
          fab auth login -u $CLIENT_ID -p $CLIENT_SECRET --tenant $TENANT_ID

      - name: Set CLI mode to interactive
        run: fab config set mode interactive

      # - name: List workspace contents
      #   run: fab ls Prod-Fabric-workspace.Workspace


      - name: Try deploying pipeline via CLI
        continue-on-error: true
        run: |

          
          # Try full Fabric path
          fab import Prod-Fabric-workspace.Workspace/pipeline_testing.DataPipeline -i deploy/pipeline/pipeline_testing/pipeline_testing.json -f



